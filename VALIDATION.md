# 验证记录

验证环境：2026-09-18，MacBook Air / Apple M5，macOS 26.6.2，Swift 6.4 Command Line Tools。

## 已完成

- `./scripts/build.sh` 成功生成独立应用 `dist/ZDuo.app`（约 750 KB），签名完整性和 Info.plist 校验通过。
- `./scripts/test.sh`：10 项 Swift Testing 测试全部通过，涵盖角度映射、铰链锚点、投影数值稳定、顶部离焦梯度、双向一致性、平滑、异常传感器报告和故障退出条件。
- Apple M5 GPU 实际编译并执行 Metal shader，在 110°、85°、60°、30°、15° 生成测试图。110° 输入／输出最大通道误差为 **0**，无透明空洞，从 15° 返回 60° 与合至 60° 的结果完全一致。
- UI 启动、默认关闭、真实传感器读数、模式切换及模拟角度调节已检查。
- 拒绝／缺少录屏权限时只显示提示，不展示覆盖层。更新当前构建的授权后，ScreenCaptureKit 捕获成功。
- 真实桌面捕获在 109°、83°、70° 模拟输入下实际运行，状态在 GPU 首帧完成后进入“实时景深”；半开停留时持续运行，控制面板保持清晰。观察到约 26–33 fps 的提交速率（新桌面帧目标为 30 fps，角度变化时额外绘制）。
- 切换回 117° 的真实角度后恢复正常桌面；再次切换 70° 模拟输入可重新启动捕获；面板开关可立即停止效果。交付时恢复真实开合模式并关闭效果。
- 修复了初次触发覆盖时的 NSWindow 初始化崩溃；修复后上述桌面验证未出现新崩溃报告。

## 仍需人工覆盖的场景

- 实体键盘触发 `⌃⌥⌘D`：系统注册成功，但自动化工具发送组合键未触发全局通道，因此没有将其记为实测通过。面板开关已验证可停用。
- 不同开合速度、观察位置下的主观景深观感。
- 真实合盖睡眠、锁屏后唤醒，以及不同外接／镜像显示器组合。相关状态退出逻辑已有单元测试，但不能替代这些硬件场景。
- 不同第三方全屏应用和受保护视频。

这份记录区分自动化、当前实机验证和待人工覆盖项，不代表所有机型兼容认证。
