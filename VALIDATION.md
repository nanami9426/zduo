# 验证记录

验证环境：2026-09-18，MacBook Air / Apple M5，macOS 26.6.2，Swift 6.4 Command Line Tools。

## 当前修订：历史渐变与 Hinge 投影组合

- 历史基线为 2026-09-18 22:44:34 的 `2919cde`，是本地 Git 中最接近用户所述 22:46 的版本，并非精确到 22:46 的独立存档。
- 恢复该版磨砂色调、覆盖强度、反光、颗粒和材质角度曲线；几何对齐 Hinge 的 0.30 taper、0.65 裁剪角系数与名义 8° 终点。保留普通设置窗口、侧边独立模糊背景、屏幕空间散射和现有传感器平滑。
- 16 项核心测试通过，新增 Hinge 在 25% / 50% / 75% / 100% 进度的四组参考坐标，以及历史材质曲线与几何进度独立的检查。
- Apple M5 实际执行 Metal：110° 至 8° 输出无透明空洞，参考角度误差 **0**，零强度还原、反向一致通过；CPU/GPU 投影通道误差 **1/255**。
- 64/255 灰底在 60° 的五行材质均值与历史参考值的最大误差为 **0.45/255**；顶部均值 **97.98**、方差 **0.27**。侧边条纹检查改为去掉平滑渐变后的高频残差，最大 **3.5/255**，通过 12/255 阈值。
- 已检查 85° / 60° / 30° 测试图与 108° 侧边条纹图，结果保存在 `.build/classic-hinge`。上述检查不证明物理空间锁定，实际开合时的“停留原地”观感仍需用户体验。
- 应用已重新构建、签名验证并启动，UI 仍为普通窗口，保留用户当前 120° 参考角度与 100% 强度，效果默认关闭。本轮未执行实时桌面录制验证；对照图位于 `.build/classic-hinge-comparison.png`。

## 此前修订：普通设置窗口

- 设置界面由浮动 `NSPanel` 改为普通层级的 `NSWindow`，移除效果开启时提升层级的订阅，以及跨桌面、附着其他应用全屏空间的行为；增加最小化。
- 设置窗口获得焦点时临时隐藏效果覆盖，失去焦点或关闭后恢复；菜单状态单独保留，避免关闭菜单时挡住仍在编辑的设置窗口。
- 菜单栏按钮可重新唤回被其他窗口遮挡或已最小化的设置界面。
- 发布构建和签名检查通过，已重启本地 app。UI 检查返回“标准窗口”，最小化按钮可用；效果保持关闭，未新增录屏授权操作。

## 此前修订：两侧文字拖尾

- 原因：透视采样越过左右边界后，`clamp_to_edge` 将桌面最边缘的一列文字像素水平重复，轻微合盖时仍保留明显条纹。
- 修复：新捕获帧生成独立的 36 pt MPS 模糊背景，图外按屏幕坐标采样背景并与虚拟屏幕柔和衔接；角度变化复用缓存，左右两侧共同修复。
- 新增贴边细线 GPU 回归图，覆盖参考 118°、实际 108° / 85° / 60°。原实现的侧边亮暗差为 **162/255**，修复后为 **7/255**，通过不超过 12/255 的阈值；已对照检查 108° 图像。
- 原有参考角度还原、零强度、双向一致、CPU/GPU 坐标和暗灰磨砂检查继续通过。输出位于 `.build/side-fill-before` 与 `.build/side-fill-after`。
- 修复版应用已构建、签名验证并重新打开。当前实际角度 108°、参考 118°；实时预览仍提示需要屏幕录制权限，因此本轮仅完成离屏 GPU 验证，未修改系统权限，交付时效果关闭。

## 此前修订：合盖幅度与磨砂材质

- `./scripts/test.sh`：15 项测试通过。新增默认角度下的可见位移阈值，以及 30° / 60° / 110° / 150° 参考角度、0 / 25% / 50% / 100% 强度下的投影稳定性；保留铰链、连续性、双向一致和快速反向检查。
- Apple M5 实际编译执行两阶段 Metal 管线，110° / 109° / 100° / 85° / 60° / 30° / 15° 离屏检查通过。参考角度通道误差 **0**，画面完全不透明，反向到 60° 逐像素一致，零强度完全还原。
- 坐标色卡在 100° / 85° / 60° / 30° / 15° 检查 CPU/GPU 逆投影，最大通道误差 **1/255**。
- 64/255 暗灰色卡在 60° 顶部中心的绿色通道均值 **70.27**、方差 **1.04**，保留暗部并出现微弱颗粒；底部铰链通道误差不超过 1。它是材质参数回归检查，不代表主观真实性的客观评分。
- 已对照检查旧源码与新版的离屏图。85° / 60° / 30° 的透视位移增强；新版先透视后做屏幕空间散射，减轻深度合盖时模糊的纵向拉伸。
- 基线保存在 `.build/hinge-frost-source-before`，新版输出在 `.build/hinge-frost-final`。现有 `dist` 中的旧 app 与初始源码不同，因此没有将旧 app 输出作为源码修改的基线。
- 最终应用已重新构建、签名验证并启动，打包后的离屏 GPU 检查同样通过。UI 可读取真实角度并切换模拟模式；85° 模拟预览因 macOS 屏幕录制权限提示而未进入捕获。未修改系统权限，交付时恢复真实开合、效果关闭，保留用户的 118° 参考角度和 100% 强度。
- 尚未完成真实开合的主观观感验收，也未测量新版持续帧率、功耗和端到端延迟。下文真实桌面捕获记录属于此前版本，不能作为新两阶段管线的实测结果。

## 此前版本已完成

- `./scripts/build.sh` 成功生成独立应用 `dist/ZDuo.app`（约 750 KB），签名完整性和 Info.plist 校验通过。
- `./scripts/test.sh`：13 项 Swift Testing 测试全部通过，涵盖角度映射、铰链锚点、投影数值稳定、顶部离焦梯度、双向一致性、平滑、异常传感器报告和故障退出条件。
- Apple M5 GPU 实际编译并执行 Metal shader，在 110°、85°、60°、30°、15° 生成测试图。110° 输入／输出最大通道误差为 **0**，无透明空洞，从 15° 返回 60° 与合至 60° 的结果完全一致。
- UI 启动、默认关闭、真实传感器读数、模式切换及模拟角度调节已检查。
- 拒绝／缺少录屏权限时只显示提示，不展示覆盖层。更新当前构建的授权后，ScreenCaptureKit 捕获成功。
- 真实桌面捕获在 109°、83°、70° 模拟输入下实际运行，状态在 GPU 首帧完成后进入“实时景深”；半开停留时持续运行，控制面板保持清晰。观察到约 26–33 fps 的提交速率（新桌面帧目标为 30 fps，角度变化时额外绘制）。
- 切换回 117° 的真实角度后恢复正常桌面；再次切换 70° 模拟输入可重新启动捕获；面板开关可立即停止效果。交付时恢复真实开合模式并关闭效果。
- 修复了初次触发覆盖时的 NSWindow 初始化崩溃；修复后上述桌面验证未出现新崩溃报告。

## 跟随延迟调整

- 固定 12 ms 平滑的版本出现用户反馈的顿挫后，改为 12–35 ms 自适应平滑，HID 恢复 30 Hz 轮询；面板角度刷新限制为 15 Hz，渲染参数保持 60 Hz 更新。硬件内部更新率和端到端显示延迟尚未测量。
- 新增连续开合测试：在 30/60/120 Hz 输入下，以 90°/s 合盖时平滑误差小于 2°，反向后三帧内转向，停止后无过冲并收敛。13 项测试通过；另测整数角度缓慢变化时，每个显示帧的步长小于 0.5°。这不是物理屏幕端到端延迟或帧率测量，实际流畅度仍需开合体验。

## 仍需人工覆盖的场景

- 新增物理屏幕磨砂层已通过 Apple M5 GPU 测试图验证：110° 完全还原，85°、60°、30°、15° 逐步增强，反向到 60° 结果一致；已检查顶部至铰链的视觉渐变。本轮尚未重新验证真实桌面上的磨砂观感，以上桌面捕获记录来自此前版本。

- 实体键盘触发 `⌃⌥⌘D`：系统注册成功，但自动化工具发送组合键未触发全局通道，因此没有将其记为实测通过。面板开关已验证可停用。
- 不同开合速度、观察位置下的主观景深观感。
- 真实合盖睡眠、锁屏后唤醒，以及不同外接／镜像显示器组合。相关状态退出逻辑已有单元测试，但不能替代这些硬件场景。
- 不同第三方全屏应用和受保护视频。

这份记录区分自动化、当前实机验证和待人工覆盖项，不代表所有机型兼容认证。

## 满高度投影修正

- 参考 Hinge 移除旧版虚拟卡片的旋转压缩，改为满高度、轻微侧边收窄和从顶部裁剪；保留物理屏幕磨砂。
- 新增几何回归检查：所有合盖角度下中心列采样始终在原图内、从底到顶严格单调、底部固定，顶部裁剪连续增强。旧版顶部会采到原图外的断言已替换。
- Apple M5 实际 GPU 输出 110°、85°、60°、30°、15° 测试图，参考角度最大通道误差 0，无透明空洞，反向一致；检查 60° 和 30° 输出，顶部不再露出旧版的大块背景填充。
- 固定位置的主观空间感仍需真实开合验收，不能用离屏截图证明空间锁定。
